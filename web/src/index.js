import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

//Jsx (JavaScript + HTML) esse <App />, precisamos importar React quando tem isso
//ReactDOM habilidade do react se comunicar com a árvore de elementos da aplicação, o html